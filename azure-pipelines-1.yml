trigger:
  - main

pr: none

resources:
  - repo: self

variables:
  - group: weather-secrets

  vmImageName: ubuntu-latest
  dockerfilePath: $(Build.SourcesDirectory)/Dockerfile
  tag: latest

stages:
  - stage: Lint
    displayName: Lint
    jobs:
      - job: LintJob
        pool:
          vmImage: $(vmImageName)
        steps:
          - checkout: self
          - task: NodeTool@0
            displayName: Use Node.js
            inputs:
              versionSpec: '20.x'
          - script: npm ci
            displayName: Install dependencies
          - script: npm run lint
            displayName: Run ESLint

  - stage: UnitTests
    displayName: Unit tests
    dependsOn: Lint
    condition: succeeded()
    jobs:
      - job: TestJob
        pool:
          vmImage: $(vmImageName)
        steps:
          - checkout: self
          - task: NodeTool@0
            displayName: Use Node.js
            inputs:
              versionSpec: '20.x'
          - script: npm ci
            displayName: Install dependencies
          # Angular/Karma needs Chrome. This setup is commonly enough for Ubuntu agents.
          - bash: |
              set -euo pipefail
              sudo apt-get update
              sudo apt-get install -y google-chrome-stable
            displayName: Install Chrome
          - bash: |
              set -euo pipefail
              export CHROME_BIN=/usr/bin/google-chrome
              npm test -- --watch=false --browsers=ChromeHeadless --code-coverage=true
              # Enforce minimum coverage (overall line coverage)
              node scripts/coverage-check.js coverage/weather/lcov.info
            displayName: Run Karma tests (headless)

  - stage: Build_Dev
    displayName: Build (Dev)
    dependsOn: UnitTests
    condition: succeeded()
    jobs:
      - job: BuildDev
        pool:
          vmImage: $(vmImageName)
        steps:
          - checkout: self
          - task: NodeTool@0
            displayName: Use Node.js
            inputs:
              versionSpec: '20.x'
          - script: npm ci
            displayName: Install dependencies
          - bash: |
              set -euo pipefail
              npm run build:dev
            displayName: Angular build (dev)

  - stage: SonarCloud
    displayName: SonarCloud analysis (gate)
    dependsOn:
      - Lint
    condition: succeeded()
    jobs:
      - job: SonarCloud
        pool:
          vmImage: $(vmImageName)
        steps:
          - checkout: self
          - task: NodeTool@0
            displayName: Use Node.js
            inputs:
              versionSpec: '20.x'
          - script: npm ci
            displayName: Install dependencies

          # Install deps + run tests once here so sonar has coverage available in this stage.
          - bash: |
              set -euo pipefail
              npm_config_loglevel=error

              sudo apt-get update
              sudo apt-get install -y google-chrome-stable
              export CHROME_BIN=/usr/bin/google-chrome

              npm ci

              npm test -- --watch=false --browsers=ChromeHeadless --code-coverage=true
              node scripts/coverage-check.js coverage/weather/lcov.info

              # Install sonar-scanner locally (no global agent tooling required)
              npm i --no-save sonar-scanner

              # Validate required env vars (fails fast with a clear message)
              if [ -z "${SONAR_TOKEN:-}" ]; then
                echo "ERROR: SONAR_TOKEN is required (create a SonarCloud token and set it as a secret pipeline variable)."
                exit 1
              fi
              if [ -z "${SONAR_ORGANIZATION:-}" ]; then
                echo "ERROR: SONAR_ORGANIZATION is required (your SonarCloud organization slug, as shown in SonarCloud)."
                exit 1
              fi
              if [ -z "${SONAR_PROJECT_KEY:-}" ]; then
                # Prefer pipeline variable if present; otherwise fallback to repo name.
                export SONAR_PROJECT_KEY="${BUILD_REPOSITORY_NAME:-weather}"
                echo "WARNING: SONAR_PROJECT_KEY not set; using fallback: ${SONAR_PROJECT_KEY}"
              fi

              # Helpful diagnostics (do NOT print token)
              echo "SonarCloud host: ${SONAR_HOST_URL}"
              echo "SonarCloud organization: ${SONAR_ORGANIZATION}"
              echo "SonarCloud projectKey: ${SONAR_PROJECT_KEY}"

              # Run analysis
              ./node_modules/.bin/sonar-scanner \
                -X \
                -Dsonar.host.url="$SONAR_HOST_URL" \
                -Dsonar.login="$SONAR_TOKEN" \
                -Dsonar.projectKey="$SONAR_PROJECT_KEY" \
                -Dsonar.projectName="$SONAR_PROJECT_KEY" \
                -Dsonar.organization="$SONAR_ORGANIZATION" \
                -Dsonar.javascript.lcov.reportPaths="coverage/weather/lcov.info"
            displayName: Sonar scanner (CLI)
            env:
              SONAR_HOST_URL: $(SONAR_HOST_URL)
              SONAR_TOKEN: $(SONAR_TOKEN)
              SONAR_PROJECT_KEY: $(SONAR_PROJECT_KEY)
              SONAR_ORGANIZATION: $(SONAR_ORGANIZATION)

  - stage: Build_Prod
    displayName: Build (Production)
    dependsOn: Build_Dev
    condition: succeeded()
    jobs:
      - job: BuildProd
        pool:
          vmImage: $(vmImageName)
        steps:
          - checkout: self
          - task: NodeTool@0
            displayName: Use Node.js
            inputs:
              versionSpec: '20.x'
          - script: npm ci
            displayName: Install dependencies
          - bash: |
              set -euo pipefail
              npm run build:prod
            displayName: Angular build (production)

  - stage: DockerPush
    displayName: Build and push Docker image
    dependsOn: Build_Prod
    condition: succeeded()
    jobs:
      - job: DockerPushJob
        displayName: Docker buildAndPush
        pool:
          vmImage: $(vmImageName)
        steps:
          - task: Docker@2
            displayName: Build and push an image to container registry
            inputs:
              command: buildAndPush
              repository: $(imageRepository)
              dockerfile: $(dockerfilePath)
              containerRegistry: $(dockerRegistryServiceConnection)
              tags: |
                $(tag)
                latest

  - stage: Deploy_AppService
    displayName: Deploy to Azure App Service (continuous deployment)
    dependsOn: DockerPush
    condition: succeeded()
    jobs:
      - job: DeployJob
        pool:
          vmImage: $(vmImageName)
        steps:
          - task: AzureCLI@2
            displayName: Update App Service container image to latest
            inputs:
              azureSubscription: weatherappres-acr-service-connection
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -euo pipefail
                az webapp config container set \
                  --name weatherappweb \
                  --resource-group WeatherApp_group \
                  --docker-custom-image-name weatherappres.azurecr.io/weather-app:latest





